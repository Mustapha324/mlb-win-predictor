import Link from "next/link";
export const metadata = { title: "The SportIQ Model" };
export default function Page() {
  return <main className="mx-auto max-w-5xl"><header className="mb-7 border-b border-white/10 pb-7"><p className="sport-kicker">The model comes first</p><h1 className="mt-3 text-4xl font-black tracking-tight sm:text-6xl">Probabilities. With context.</h1><p className="mt-4 max-w-2xl text-sm leading-7 text-neutral-400">SportIQ combines trained team models with available pregame sportsbook probabilities to estimate MLB and NFL win chances. Explore the inputs, evaluation, and limits before making your call.</p></header><div className="grid gap-5 md:grid-cols-2">{[{ sport: "MLB", href: "/metrics", text: "A trained logistic model combines team form, scoring, and Elo strength. Probable starters adjust the model estimate; available pregame sportsbook odds anchor the served probability. Game Brain runs separately as an experimental explanation layer." }, { sport: "NFL", href: "/nfl/metrics", text: "A trained model artifact scores team features built from completed games. When a pregame line is available, the served estimate blends 25% model and 75% market in logit space. Without one, the model stands alone." }].map((item) => <section className="sport-panel p-7" key={item.sport}><p className="sport-kicker">{item.sport}</p><h2 className="mt-3 text-2xl font-black">Inside the {item.sport} model</h2><p className="mt-4 text-sm leading-7 text-neutral-400">{item.text}</p><Link className="primary-button mt-6" href={item.href}>Methodology & performance</Link></section>)}</div><section className="sport-panel mt-5 p-7"><h2 className="text-xl font-black">What a prediction means</h2><p className="mt-3 text-sm leading-7 text-neutral-400">A 64% probability is an estimate, not a guarantee. Historical accuracy, Brier score, and log loss provide different views of performance where reported. Pregame snapshots stay fixed so later events cannot rewrite the original prediction. Your personal stats compare SportIQ’s saved call with your own selection on the same graded games.</p><div className="mt-5 flex flex-wrap gap-3"><Link href="/history" className="secondary-button">MLB results</Link><Link href="/nfl/history" className="secondary-button">NFL results</Link><Link href="/my-stats" className="secondary-button">Compare with the model</Link></div></section></main>;
}
